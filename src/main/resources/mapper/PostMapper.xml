<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.graduation.interviewAi.mapper.PostMapper">

    <!-- 게시글 목록 조회 (최신순) -->
    <select id="selectAllPosts" resultType="com.graduation.interviewAi.domain.Post">
        SELECT * FROM Post ORDER BY createdAt DESC
    </select>
    
    <!-- 게시글 등록 -->
    <insert id="insertPost" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO Post (postName, pContent, userId, userRole, createdAt)
        VALUES (#{postName}, #{pContent}, #{userId}, #{userRole}, NOW())
    </insert>
    
    <!-- 게시글 상세 조회 -->
	<select id="getPostById" resultType="com.graduation.interviewAi.domain.Post">
	    SELECT * FROM Post WHERE postId = #{postId}
	</select>
	
	<!-- postId로 댓글 목록 조회 -->
	<select id="getCommentsByPostId" resultType="com.graduation.interviewAi.domain.Comment">
	    SELECT * FROM Comment WHERE postId = #{postId} ORDER BY createdAt ASC
	</select>

	<!-- 게시글 수정 -->
	<update id="updatePost">
	    UPDATE Post
	    SET postName = #{postName},
	        pContent = #{pContent},
	        userRole = #{userRole}
	    WHERE postId = #{postId}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deletePost">
    DELETE FROM Post WHERE postId = #{postId}
	</delete>

</mapper>
